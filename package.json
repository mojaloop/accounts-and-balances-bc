{
  "name": "@mojaloop/accounts-and-balances-bc",
  "version": "0.0.1",
  "description": "Accounts and balances bounded context.",
  "license": "Apache-2.0",
  "homepage": "https://github.com/mojaloop/accounts-and-balances-bc",
  "bugs": {
    "url": "https://github.com/mojaloop/accounts-and-balances-bc/issues#readme"
  },
  "contributors": [
    "Gonçalo Garcia <goncalogarcia99@gmail.com>",
    "Pedro Sousa Barreto <pedrob@crosslaketech.com>"
  ],
  "scripts": {
    "start": "yarn workspaces foreach --parallel -v run start",
    "start:web-service-app": "yarn workspace @mojaloop/accounts-and-balances-bc-web-service-app run start",
    "build": "yarn workspaces foreach -v run build",
    "watch": "yarn workspaces foreach --parallel -v run watch",
    "clean": "yarn run clean:dist",
    "clean:npm": "yarn workspaces foreach --parallel -v run clean:npm",
    "clean:dist": "yarn workspaces foreach --parallel -v run clean:dist",
    "lint": "yarn workspaces foreach -v run lint",
    "lint:fix": "yarn workspaces foreach -v run lint:fix",
    "test:unit": "yarn workspaces foreach -v run test:unit",
    "test:integration": "yarn workspaces foreach --parallel -v run test:integration",
    "test:coverage": "yarn workspaces foreach --parallel -v run test:coverage",
    "test:coverage-check": "yarn workspaces foreach --parallel -v run test:coverage-check",
    "publish:client-lib": "yarn workspace @mojaloop/accounts-and-balances-bc-client run publish",
    "publish:private-types-lib": "yarn workspace @mojaloop/accounts-and-balances-bc-private-types run publish"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.9",
    "@types/node-cache": "^4.2.5",
    "@typescript-eslint/eslint-plugin": "^5.22.0",
    "@typescript-eslint/parser": "^5.22.0",
    "eslint": "^8.14.0",
    "husky": "^8.0.0",
    "jest": "^27.3.1",
    "npm-check-updates": "^12.5.11",
    "ts-jest": "^27.0.7",
    "typescript": "^4.6.4"
  },
  "workspaces": [
    "modules/*"
  ],
  "packageManager": "yarn@3.2.1",
  "engines": {
    "yarn": "3.2.1"
  }
}
