{
  "name": "@mojaloop/accounts-and-balances-bc",
  "version": "0.0.1",
  "description": "Accounts and Balances Bounded Context",
  "license": "Apache-2.0",
  "homepage": "https://github.com/mojaloop/accounts-and-balances-bc",
  "bugs": {
    "url": "https://github.com/mojaloop/accounts-and-balances-bc/issues#readme"
  },
  "contributors": [
    "Gonçalo Garcia <goncalogarcia99@gmail.com>",
    "Pedro Sousa Barreto <pedrob@crosslaketech.com>"
  ],
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "clean:dist": "npm -ws -p run clean:dist",
    "clean:npm": "rm -fr node_modules && npm -ws -p run clean:npm",
    "clean:package-lock": "rm -f package-lock.json && npm -ws -p run clean:package-lock",
    "clean:coverage": "rm -fr test/coverage && npm -ws -p run clean:coverage",
    "clean:all": "npm run clean:dist && npm run clean:npm && npm run clean:package-lock && npm run clean:coverage",
    "dep:check": "ncu -e 2 && npm -ws -p run dep:check",
    "dep:upgrade": "ncu -u && npm -ws -p run dep:upgrade",
    "lint": "npm -ws -p run lint",
    "build": "npm -ws -p run build",
    "test:unit": "npm -ws -p run test:unit",
    "test:integration": "jest test/integration"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@typescript-eslint/eslint-plugin": "^5.36.2",
    "@typescript-eslint/parser": "^5.36.2",
    "eslint": "^8.23.0",
    "husky": "^8.0.1",
    "jest": "^28.0.0",
    "npm-check-updates": "^16.1.0",
    "ts-jest": "^28.0.8",
    "typescript": "^4.8.2"
  },
  "workspaces": [
    "packages/domain-lib",
    "packages/infrastructure-lib",
    "packages/*"
  ]
}
