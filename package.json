{
  "name": "@mojaloop/accounts-and-balances-bc",
  "version": "0.0.1",
  "description": "Accounts and Balances Bounded Context",
  "license": "Apache-2.0",
  "homepage": "https://github.com/mojaloop/accounts-and-balances-bc",
  "bugs": {
    "url": "https://github.com/mojaloop/accounts-and-balances-bc/issues#readme"
  },
  "contributors": [
    "Gonçalo Garcia <goncalogarcia99@gmail.com>",
    "Pedro Sousa Barreto <pedrob@crosslaketech.com>"
  ],
  "scripts": {
    "prepare": "husky install",
    "clean": "npm -ws -p run clean",
    "clean:dist": "npm -ws -p run clean:dist",
    "clean:node-modules": "rm -rf node_modules && npm -ws -p run clean:node-modules",
    "clean:dist-and-node-modules": "rm -rf node_modules && npm -ws -p run clean:dist-and-node-modules",
    "dep:check": "npm -ws -p run dep:check",
    "lint": "npm -ws -p run lint",
    "build": "npm -ws -p run build",
    "clean-and-build": "npm run clean && npm run build",
    "test:unit": "npm -ws -p run test:unit",
    "publish": "npm -ws -p run publish"
  },
  "devDependencies": {
    "@types/jest": "^28.1.7",
    "@typescript-eslint/eslint-plugin": "^5.33.1",
    "@typescript-eslint/parser": "^5.33.1",
    "eslint": "^8.22.0",
    "husky": "^8.0.1",
    "jest": "^28.1.3",
    "npm-check-updates": "^16.0.5",
    "ts-jest": "^28.0.8",
    "typescript": "^4.7.4"
  },
  "workspaces": [
    "packages/infrastructure-lib",
    "packages/domain-lib",
    "packages/*"
  ]
}
