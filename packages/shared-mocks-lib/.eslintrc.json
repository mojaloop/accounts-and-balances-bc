{
  "extends": [
    "../../.eslintrc.json"
  ],
  "rules": { // Added to the ones specified on the parent file.
    // Many mock functions don't do anything with their parameters (they're only there to respect an interface).
    "@typescript-eslint/no-unused-vars": [
      "off"
    ]
  },
  "overrides": [
    {
      "files": [
        ""
      ],
      "rules": {
      }
    }
  ]
}